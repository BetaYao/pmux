# Bug Report — 2026-03-17 Post-Refactoring

**Test Results:** 604 passed, 24 failed

## Bug Categories

### 1. Config: Agent detect count mismatch (3 tests)
- `test_agent_detect_default_config` — expects 4, got 5
- `test_agent_detect_backward_compat` — expects 4, got 5
- `test_agent_detect_serialization_roundtrip` — expects 4, got 5

**Root cause:** A new agent detect variant was added but tests still assert old count (4).
**Fix:** Update test assertions to expect 5.
**File:** `src/config.rs`

### 2. Help panel: Item count mismatch (2 tests)
- `test_help_panel_default` — expects 10 items, got 27
- `test_search_filtering` — expects 10 items, got 27

**Root cause:** More keybindings/help items added; tests hardcode old count.
**Fix:** Update assertions to 27 (or make tests count-independent).
**File:** `src/help_panel.rs`

### 3. Hooks installer: Extra newline in TOML block removal (1 test)
- `test_remove_toml_pmux_block` — output has extra blank line between "before" and "after"

**Root cause:** `remove_toml_pmux_block` leaves extra newline when removing block.
**Fix:** Adjust removal logic to collapse consecutive blank lines, or update test expectation.
**File:** `src/hooks/installer.rs`

### 4. New branch dialog (2 tests)
- `test_new_branch_dialog_cannot_close_while_creating` — `dialog.is_open()` returns false
- `test_validate_branch_name_rejects_spaces` — leading-space branch name passes validation

**Root cause:** Dialog state management or validation regex changed during refactoring.
**Fix:** Check `is_open()` logic and fix `validate_branch_name` to reject leading spaces.
**Files:** `src/new_branch_dialog.rs`

### 5. Legacy tmux runtime (4 tests)
- `test_tmux_runtime_attach_nonexistent_session` — `result.is_err()` fails
- `test_tmux_runtime_attach_requires_tmux` — same
- `test_tmux_runtime_list_panes_requires_tmux` — panes not empty
- `test_tmux_runtime_send_key_no_process_spawn` — send_key fails

**Root cause:** Legacy tmux runtime API was changed during refactoring.
**Fix:** Update tests to match new API, or fix legacy runtime methods.
**File:** `src/runtime/backends/tmux.rs`

### 6. Terminal input: key_char_sends_bytes (1 test)
- `test_key_char_sends_bytes` — returns `None` instead of `Some([97])`

**Root cause:** `key_to_bytes` or equivalent function signature changed.
**Fix:** Update function or test to match new signature.
**File:** `src/terminal/input.rs`

### 7. Terminal core: All output is empty string (5 tests)
- `test_process_output_split_utf8_renders_correctly` — expects '编', got ""
- `test_process_output_split_utf8_3way` — same
- `test_process_output_mixed_ascii_and_split_utf8` — expects 'hi编码', got ""
- `test_fast_typing_no_text_replacement` — expects 'claude', got ""
- `test_concurrent_process_output_and_resync_no_corruption` — expects 'claude', got ""

**Root cause:** Terminal core refactoring broke process output → screen rendering pipeline. Screen content extraction returns empty.
**Fix:** Check `process_output` and screen content readback in terminal_core.
**File:** `src/terminal/terminal_core.rs`

### 8. Window state (2 tests)
- `test_persistent_app_state_default` — default sidebar width changed from 250 to 280
- `test_auto_save_manager` — `should_save()` returns false

**Root cause:** Default values or timing logic changed during refactoring.
**Fix:** Update default width assertion; check `should_save` timing/dirty logic.
**File:** `src/window_state.rs`

### 9. Worktree manager: Parse detached (1 test)
- `test_parse_worktree_list_detached` — expects 2 entries, got 1

**Root cause:** Parsing logic change skips detached HEAD entries.
**Fix:** Check parser for detached worktree handling.
**File:** `src/worktree_manager.rs`

### 10. Tmux control mode (3 tests)
- `test_resolve_pane_tty_returns_file` — can't resolve pane TTY for %0
- `test_direct_write_cache_miss_then_hit` — `direct_write` returns error
- `test_hex_send_keys_roundtrip` — output is empty

**Root cause:** Tmux control mode refactoring broke pane TTY resolution and direct write paths.
**Fix:** Check `resolve_pane_tty`, `direct_write`, and hex send-keys implementation.
**File:** `src/runtime/backends/tmux_control_mode.rs`

---

## Priority Recommendation

1. **High** — Terminal core (#7): 5 tests, core rendering pipeline broken
2. **High** — Terminal input (#6): key input broken
3. **Medium** — Tmux control mode (#10): 3 tests, primary backend
4. **Medium** — New branch dialog (#4): user-facing validation bug
5. **Low** — Config (#1), Help panel (#2), Window state (#8): just count/default mismatches
6. **Low** — Legacy tmux (#5): deprecated backend
7. **Low** — Hooks installer (#3), Worktree manager (#9): minor
